---
const { lists } = Astro.props;
---
{lists.map((item) => (
  <p>{item.language}</p>
  <div class="w3-light-grey w3-round-xlarge">
    <div class="w3-round-xlarge w3-teal " style={` text-align: center;height:24px;width:${item.percentage}%`}> بطلاقة</div>
  </div>
))}